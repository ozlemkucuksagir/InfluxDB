
package com.example.influxdb.exception.request;
/*
import org.aspectj.lang.JoinPoint;
import org.aspectj.lang.annotation.AfterReturning;
import org.aspectj.lang.annotation.Aspect;
import org.aspectj.lang.annotation.Before;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Component;
@Aspect
@Component
public class RequestSuccessLoggerAspect {

    private static final Logger logger = LoggerFactory.getLogger(RequestSuccessLoggerAspect.class);

    @Before("execution(* com.example.influxdb.controller.*.*(..))")
    public void logSuccess(JoinPoint joinPoint) {
        String methodName = joinPoint.getSignature().getName();
        String url = joinPoint.getArgs()[0].toString(); // assuming the first argument is the URL
        logger.info("URL:" + url + ";Request Status:200");
    }
}
*/